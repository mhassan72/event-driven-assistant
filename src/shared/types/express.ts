/**
 * Express Type Extensions
 * Extended types for Express Request and Response
 */

// Use any to avoid TypeScript conflicts with the global Express namespace
export type Request = any;
export type Response = any;