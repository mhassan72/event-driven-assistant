/**
 * Features Test Index
 * Aggregates all feature tests
 */

import './ai-assistant/index.test';

describe('Features', () => {
  it('should have all feature tests available', () => {
    // This test ensures all feature test modules are properly imported
    expect(true).toBe(true);
  });
});